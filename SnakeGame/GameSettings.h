#pragma once

constexpr float SPEED = 30.0f;
constexpr float SPEED_FACTOR = 0.65f;
constexpr int BOARD_WIDTH = 40;
constexpr int BOARD_HEIGHT = BOARD_WIDTH;
constexpr int TILE_SIZE = 64 * (1.0f / (BOARD_WIDTH / 20.0f));